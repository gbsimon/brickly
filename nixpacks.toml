# Nixpacks configuration to ensure Node.js 22.x is used
# This pins to a nixpkgs commit that includes Node.js 22.13.1+
# which matches the project's Node.js engine requirement
[phases.setup]
nixpkgsArchive = "51ad838b03a05b1de6f9f2a0fffecee64a9788ee"
